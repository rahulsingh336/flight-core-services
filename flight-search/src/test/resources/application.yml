sample:
    #port: ${wiremock.server.port}
    #port: ${stubrunner.runningstubs.stock.port}
    #port1: ${wiremock.port}

wiremock:
    server:
        port-dynamic: true

#baseurl: http://localhost:${stubrunner.runningstubs.stock.port}
external:
    services:
        notifications1: http://localhost:${stubrunner.runningstubs.stock.port}/v1/notification
        notifications2: http://localhost:${stubrunner.runningstubs.stock.port}/v1/notification
        notifications3: http://customer-service/notifications3
        notifications4: http://customer-service/notifications4
        notifications5: http://customer-service/notifications5

#stubrunner:
 #   idsToServiceIds:
  #      stock: emirates-search-service
   #     stock: emirates-search-service
